"use strict";
// import express from "express";
// import authenticateToken from "../middleware/authentication";
// import {
// 	createQuizeRoute,
// 	deleteQuizeRoute,
// 	increaseImpressionCount,
// } from "../controller/quizeController";
// const quizeRouter = express.Router();
// quizeRouter.post("/quize/create", authenticateToken, createQuizeRoute);
// quizeRouter.delete("/quize/delete/:id", deleteQuizeRoute);
// quizeRouter.put("/quize/increase/:id", increaseImpressionCount);
// export default quizeRouter;
